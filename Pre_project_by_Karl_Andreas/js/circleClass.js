class Circle {                                   // Circle class
    constructor(x, y, r) {
        this.x = x;
        this.y = y;
        this.r = r;
        this.fill = undefined;
        this.angle = Math.random() * 2 * Math.PI;
    }
}